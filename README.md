# Emby to Trakt

Sync watched history from Emby to Trakt.

## Installation

```bash
uv pip install -e .
```

## Usage

```bash
emby-sync setup      # Configure Emby connection
emby-sync download   # Download watched history
emby-sync status     # Show sync status
emby-sync validate   # Test connection
```
